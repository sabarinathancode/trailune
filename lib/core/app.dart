import 'package:flutter/material.dart';

class TrailuneApp extends StatelessWidget {
  const TrailuneApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Trailune',
      home: Scaffold(body: Center(child: Text('Trailune'))),
    );
  }
}
